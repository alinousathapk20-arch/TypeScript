const numbers: number[] = [10, 21, 200, 30, 43, 50];
/* 
const max = numbers.reduce(
  (acc, current) => (current > acc ? current : acc),
  numbers[0]
); */

console.log(max);

//console.log(numbers.sort((a, b) => a - b));
